
function GabledRoof(properties, coordinates, center) {
  var roof = RidgedRoof(properties, coordinates[0], center, 0);
  return { coordinates:[outerRing], ridge:ridge };

  if (offset === 0) {
    var index = cap1;
    outerRing = outerRing.splice(index, 1, c1);
    index = cap2 + (cap2 > index ? 1 : 0);
    outerRing = outerRing.splice(index, 1, c2);
    return [c1, c2];
  }



}

// TODO: handle ridge vs. height coords